# Repository audit — 2026-08-27

CodeAndRoll2 is an R function-library package. The audit covered the package source, generated
documentation, development configuration, namespace, dependency inventory, and repository call sites.
Generated files were checked against their upstream sources before changes were planned.

## Logical and critical bugs

- [x] `R/CodeAndRoll2.R`, `getRows()` and `getCols()` — `remove0only = TRUE` reads undefined `mx` and always fails unless a caller happens to define `mx`; **severity: broken functionality**.
- [x] `R/CodeAndRoll2.R`, `getRows()` and `getCols()` — missing-name totals use the set difference in the wrong direction; **severity: reports incorrect results without changing the returned values**.
- [ ] `R/CodeAndRoll2.R`, `rescale()` — constant finite input divides by zero; **severity: changes all numeric output to `NaN`; intended constant-output behavior is unknown**.
- [ ] `R/CodeAndRoll2.R`, `geomean()` — zero handling needs domain-specific review; **severity: may change scientific numeric results, so no change was made**.

## Broken functionality

- [ ] `R/CodeAndRoll2.R`, `movingAve()` and `movingSEM()` — windows can address positions outside the vector; **severity: returns leading gaps and edge `NA` values; intended edge behavior is unknown**.
- [ ] `R/CodeAndRoll2.R`, `translate()` — `1:length(old)` iterates for an empty dictionary; **severity: empty input can fail instead of returning `vec` unchanged**.
- [ ] `R/CodeAndRoll2.R`, `any.duplicated.rownames.ls.of.df()` — checks `rownames(ls)` instead of row names inside each data frame; **severity: does not perform the documented operation; expected return shape is unknown**.

## Dependencies

- [ ] `Development/Dependencies.R`, unresolved calls — `FirstCol2RowNames()`, `llprint()`, `qbarplot()`, and `which.max.multi()` have no resolved provider in the generated dependency inventory; **severity: affected functions fail unless another package or the global environment supplies them**.
- [ ] `Development/config.R` and `DESCRIPTION` — repository guidance names ReadWriter as a dependency, but package metadata does not; **severity: downstream calls may fail; the external function inventory was not available in this checkout, so no dependency was guessed**.

## Text and documentation

- [ ] `R/CodeAndRoll2.R`, `rowsplit()` — description says that the factor corresponds to columns; **severity: cosmetic and misleading documentation**.
- [ ] `R/CodeAndRoll2.R`, `union.ls()` — description says “Intersect”; **severity: cosmetic and misleading documentation**.

## Refactoring

- [ ] `R/CodeAndRoll2.R`, multiple loops — replace `1:length(x)` with `seq_along(x)` only while reviewing each function's empty-input contract; **severity: several functions mishandle empty input**.

## Function naming and missing features

- No safe rename or missing feature was identified. Renames require separate compatibility PRs with deprecated wrappers.
